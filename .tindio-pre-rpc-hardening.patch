diff --git a/scripts/generate-local-database-types.mjs b/scripts/generate-local-database-types.mjs
index 02aa66b..e664a83 100644
--- a/scripts/generate-local-database-types.mjs
+++ b/scripts/generate-local-database-types.mjs
@@ -49,7 +49,12 @@ if (!generated.includes("current_profile_id")) {
   process.exit(1);
 }
 
-const output = `${generated.trimEnd()}\n`;
+const output = `${generated.trimEnd()}
+
+// Application convenience alias retained across Supabase CLI type regeneration.
+export type TableRow<TableName extends keyof DefaultSchema["Tables"]> =
+  Tables<TableName>;
+`;
 
 await writeFile(
   new URL(
diff --git a/src/app/(back-office)/back-office/replenishment/page.tsx b/src/app/(back-office)/back-office/replenishment/page.tsx
index 9c55813..651607c 100644
--- a/src/app/(back-office)/back-office/replenishment/page.tsx
+++ b/src/app/(back-office)/back-office/replenishment/page.tsx
@@ -178,14 +178,14 @@ export default async function ReplenishmentPage({
     supabase.from("categories").select("id, name").eq("organization_id", organizationId).eq("is_archived", false).order("name"),
     activeTab === "levels"
       ? supabase.rpc("get_inventory_stock_page", {
-          requested_category_id: stockFilters.categoryId,
+          requested_category_id: stockFilters.categoryId ?? undefined,
           requested_page: stockPage,
           requested_page_size: stockPageSize,
           requested_restock_policy: stockFilters.restockPolicy,
-          requested_search: stockFilters.search || null,
+          requested_search: stockFilters.search || undefined,
           requested_sort: stockFilters.sort,
           requested_status: stockFilters.status,
-          requested_store_id: storeScope.selectedStoreId,
+          requested_store_id: storeScope.selectedStoreId ?? undefined,
           target_organization_id: organizationId,
         })
       : Promise.resolve({ data: null, error: null }),
diff --git a/src/features/catalog/data.ts b/src/features/catalog/data.ts
index 5799cff..cc0c315 100644
--- a/src/features/catalog/data.ts
+++ b/src/features/catalog/data.ts
@@ -8,6 +8,12 @@ import type {
 import type { BusinessContext } from "@/lib/auth/dal";
 import { createClient } from "@/lib/supabase/server";
 
+function normalizeRestockPolicy(
+  value: string,
+): "restock" | "do_not_restock" {
+  return value === "do_not_restock" ? value : "restock";
+}
+
 async function loadCatalogCostEntries(
   supabase: Awaited<ReturnType<typeof createClient>>,
   organizationId: string,
@@ -114,7 +120,10 @@ export async function loadCatalogWorkspace(
     stores: storesResult.data ?? [],
     products,
     variants: variantsResult.data ?? [],
-    settings: settingsResult.data ?? [],
+    settings: (settingsResult.data ?? []).map((setting) => ({
+      ...setting,
+      restock_policy: normalizeRestockPolicy(setting.restock_policy),
+    })),
     costs,
     inventoryLevels: inventoryLevelsResult.data ?? [],
     units: unitsResult.data ?? [],
diff --git a/src/features/inventory/advanced-inventory-actions.ts b/src/features/inventory/advanced-inventory-actions.ts
index f39470d..b89767a 100644
--- a/src/features/inventory/advanced-inventory-actions.ts
+++ b/src/features/inventory/advanced-inventory-actions.ts
@@ -686,7 +686,9 @@ export async function recordInventoryAdjustmentV2Action(
     target_reason_code: parsed.data.reasonCode,
     target_note: parsed.data.note,
     target_operation_id: parsed.data.operationId,
-    target_approval_request_id: parsed.data.approvalRequestId ?? null,
+    ...(parsed.data.approvalRequestId
+      ? { target_approval_request_id: parsed.data.approvalRequestId }
+      : {}),
   });
   if (error || !data) return { ok: false, message: databaseMessage(error?.code, "TINDIO could not post the stock adjustment.") };
   revalidatePath("/back-office/inventory");
diff --git a/src/lib/supabase/database.types.ts b/src/lib/supabase/database.types.ts
index 68ec059..98feede 100644
--- a/src/lib/supabase/database.types.ts
+++ b/src/lib/supabase/database.types.ts
@@ -8200,3 +8200,7 @@ export const Constants = {
     Enums: {},
   },
 } as const
+
+// Application convenience alias retained across Supabase CLI type regeneration.
+export type TableRow<TableName extends keyof DefaultSchema["Tables"]> =
+  Tables<TableName>;
