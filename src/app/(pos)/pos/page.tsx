import {
  PosV2TerminalShell,
} from "@/features/pos/pos-v2-terminal-shell";

export const metadata = { title: "Point of sale" };

export default function PosPage() {
  return (
    <PosV2TerminalShell />
  );
}
