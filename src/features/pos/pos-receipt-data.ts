// CANDIDATE_FOR_REMOVAL after the next release: retained only to avoid an
// automatic deletion during the navigation improvement. POS receipt readers
// now live in the approved server-only `src/features/pos/data.ts` layer.
export {};
